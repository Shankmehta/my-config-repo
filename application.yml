server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Shankmehta/my-config-repo.git
          clone-on-start: true
          skip-ssl-validation: true
application:
  name: config-server
